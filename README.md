# BOYUÉ Home Essentials

A responsive React + Vite storefront prototype for 博裕貿易發展有限公司.

## Run

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.

### Windows PowerShell

If PowerShell blocks `npm.ps1` with an execution-policy error, use the Windows command shim instead:

```powershell
npm.cmd install
npm.cmd run dev
```

## Demo admin

- Email: `admin@boyuehome.com`
- Password: `Boyue2026!`

The checkout UI is wired as a demo flow. Connect PayPal Checkout or a PCI-compliant card processor before accepting live payments. Replace the legal registration number placeholder in `index.html` with the company's verified registration number before launch.
